
import Foundation

let MAJOR_VERSION: UInt = 1
let MINOR_VERSION: UInt = 0

// a version value that can be compared. a higher number is a newer version
let VERSION_CODE = MAJOR_VERSION * 1000 + MINOR_VERSION

let VERSION_STRING = "\(MAJOR_VERSION).\(MINOR_VERSION)"

